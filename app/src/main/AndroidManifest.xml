<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="be.howest.pekka.roeselarevrijwilligt">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- <meta-data android:name="android.app.searchable" -->
        <!-- android:resource="@xml/searchable"></meta-data> -->
        <activity android:name=".MainActivity" />
        <activity android:name=".About" />
        <activity android:name=".about_Us" />
        <activity android:name=".Volunteer" />
        <activity android:name=".view_vacancies" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".matched_vacancies" />
        <activity
            android:name=".NavigationDrawerActivity"
            android:label="@string/Roeselarevrifwilligt"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Watisvrijwilligen" />
        <activity android:name=".MyExpandableListAdapter" />
        <activity android:name=".ChildClicked" />
    </application>

</manifest>